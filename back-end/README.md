## Install these Backend Dependancies ##

--pip install flask-restful

--pip install Flask Flask-SQLAlchemy Flask-Migrate Flask-JWT-Extended Flask-Bcrypt Flask-CORS psycopg2-binary python-dotenv

## Install these Frontend Dependancies ##
(("Make sure you are in the AJALI folder..e.g. Home/.../client/AJALI/))

-- npm install
